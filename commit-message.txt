fix: load TTS settings from Supabase and keep them in sync

- Add Supabase as primary source for TTS settings
- Add periodic check for settings updates
- Keep localStorage as fallback
- Add useAuth hook integration
- Ensure OpenAI TTS settings are properly loaded
- Add better error handling and logging

The app should now properly load and maintain TTS settings
from Supabase, ensuring consistent voice settings across
all pages without requiring settings page visits.
